version: '3'
services:
    web:
        build: .
        volumes:
            - ./app:/app
            - ./uploads:/uploads
        ports: ["80:80"]
        environment: 
            - API=http://py:8080/api
networks:
    default:
        external:
            name: aiir_network
